# Code-Agent 实战备忘录 (Notes)

> 记录真实踩坑、实验数据与核心架构认知。随手记，重沉淀，不搞形式主义。

---

## 2026-09-23 · W1 S3 实验与认知复盘

### 1. 长上下文与 Token 边界压测数据 (test_context_limit.py)

| 实验组别 | 代码行数 | 埋点位置 | 输入 Token | 耗时 (s) | 准确率判定 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **基准组** | 50 行 | 第 20 行 | 645 tok | 0.96s | ✅ 100% 正确命中 7 |
| **中规模组** | 797 行 | 第 461 行 | 8,616 tok | 1.40s | ✅ 100% 正确命中 7 |
| **大规模组** | 2498 行 | 第 1448 行 | 26,825 tok | 1.31s | ✅ 100% 正确命中 7 |

* **实测结论**：
  * 现代长上下文模型（如 DeepSeek-Flash）在单文件 2500 行内的单点定向检索能力极强，不会轻易发生信息遗漏；
  * **代价与硬伤**：Token 消耗从 645 飙升至 26,825（暴增 **41.5 倍**），全量硬塞极易触发 API 云厂商的每分钟 Token 配额（TPM 429 熔断）。全工程硬塞在成本和并发上完全不可行。

---

### 2. 真实痛点：错误信息污染与注意力惯性 (Sycophancy)

* **现象**：排查 Bug 时若人类先入为主给出了错误排查线索，模型会倾向于“阿谀奉承”顺水推舟，在错误方向死循环。后续即便纠偏，也因历史中充斥着错误先验知识而无法自拔；单开新窗口能秒解，本质是清除了被污染的历史。
* **架构启示**：
  * 不能只依赖死板的单向消息列表（`messages: list`）；
  * 后续阶段需引入：**上下文剪枝治理**（剔除推理噪音）与 **LangGraph 状态图的 Checkpointer 快照回滚**（走入死胡同自动回退到干净状态，程序自动实现“单开纯净窗口”）。

---

### 3. Agent 消化大项目的本质模式

* **障眼法**：成熟 Agent 工具（Cursor/Claude Code 等）面对几千个类，**从来没有一口吞下全量代码**，而是依靠外部工程架构在幕后完成精准挑拣。
* **底层机制**：**ReAct 工具调用循环**（Thought 思考 $\to$ Action 调工具 $\to$ Observation 拿到片段 $\to$ Feedback 喂回思考）。大模型像人类程序员一样自己调用搜索工具和文件读取工具，每次输入上下文的自始至终只有命中的几百行核心片段。
* **下一阶段目标**：周末 W 启动第一块外部工程基石——手写 `rank_bm25` 代码倒排索引器。
